🔟 Java Inheritance & Exception Practice Questions
1. Parent–Child Class Basics
Create a class Parent with a method showMessage() that prints "This is parent class".

Create a subclass Child with a method showChildMessage() that prints "This is child class".

Demonstrate:

Calling parent method using parent object.

Calling child method using child object.

Calling parent method using child object.

Add a private field name in Parent with getter and setter.

2. Vehicle–Car Override
Create a class Vehicle with method vehicleRun() that prints "Vehicle is running".

Create a subclass Car that overrides vehicleRun() to print "Car is running".

In main, call both methods.

Add a private field speed in Car with getter/setter.

Throw an exception if speed is negative.

3. Animal–Dog Example
Create a class Animal with method sound() that prints "Animal makes sound".

Create subclass Dog that overrides sound() to print "Dog barks".

Add a private field age in Dog with getter/setter.

Throw an exception if age < 0.

4. Bank–Account Example
Create a class Bank with method displayBankName().

Create subclass Account with private field balance.

Add getter/setter for balance.

Throw an exception if balance < 0.

Demonstrate calling parent and child methods.

5. Shape–Circle Example
Create a class Shape with method draw().

Create subclass Circle that overrides draw().

Add private field radius with getter/setter.

Throw an exception if radius <= 0.

6. Employee–Manager Example
Create a class Employee with method work().

Create subclass Manager that overrides work().

Add private field salary with getter/setter.

Throw an exception if salary < minimum wage (e.g., 5000).

7. Device–Phone Example
Create a class Device with method turnOn().

Create subclass Phone that overrides turnOn().

Add private field model with getter/setter.

Throw an exception if model is empty.

8. Student–Graduate Example
Create a class Student with method study().

Create subclass Graduate that overrides study().

Add private field grade with getter/setter.

Throw an exception if grade is invalid (not between A–F).

9. Animal–Cat Example
Create a class Animal with method eat().

Create subclass Cat that overrides eat().

Add private field food with getter/setter.

Throw an exception if food is null.

10. Appliance–WashingMachine Example
Create a class Appliance with method start().

Create subclass WashingMachine that overrides start().

Add private field capacity with getter/setter.

Throw an exception if capacity <= 0.
